```bash
$ dnsenum [--dnsserver <DNS_IP/adres>] [-f </usr/share/dnsenum/.>] [-r] <domain># enumerate domain (-r = Recursion on subdomains, brute force all discovered subdomains)
```
```bash
$ dnsenum -p 20 -s 100 --threads 5 megacorpone.com # Get extra names and subdomains via google scraping
```