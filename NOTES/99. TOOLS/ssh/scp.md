cp by ssh
```bash
$ scp [-i key_file] <source> <target>
$ scp <file/directory> <user>@<remote_Host>:<target/[new_filename/dirname]>
$ scp <user>@<remote_Host>:<target/[filename/dirname]> <file/directory>
```